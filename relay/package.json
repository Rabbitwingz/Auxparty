{
  "name": "auxparty-relay",
  "version": "1.0.0",
  "description": "WebSocket relay between browsers and the Android app (Cloudflare Worker + Durable Objects).",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test --test-concurrency=1 test/*.test.js"
  },
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.132.0",
    "ws": "^8.21.3"
  },
  "private": true
}
